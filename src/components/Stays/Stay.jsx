import './Stay.scss';

const Stay = ({
  stay: {
    city,
    country,
    superHost,
    title,
    rating,
    maxGuests,
    type,
    beds,
    photo,
  },
}) => {
  return <div>Stay</div>;
};

export default Stay;
